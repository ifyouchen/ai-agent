# 小说功能实现进度记录

版本：V1.0  
日期：2026-06-20  
当前阶段：V1 原型实现阶段  
当前目标：实现写小说、改小说、小说转短剧分场稿的最小闭环

## 1. 状态总览

| 模块 | 状态 | 备注 |
|---|---|---|
| 产品方案 | 已完成 | `产品方案-小说写改编一体化.md` |
| 设计方案 | 已完成 | `设计方案-小说转短剧脚本.md` |
| `小说skill` 能力迁移清单 | 已完成 | `小说skill能力迁移清单.md` |
| V1 实现任务清单 | 已完成 | `小说功能实现任务清单.md` |
| V1 执行说明 | 已完成 | `小说功能V1执行说明.md` |
| 前端创作入口 | 已完成 | 已新增顶部 `创作` 导航和 `/creation` |
| 作品库 | 已完成 | 已新增作品库页面、新建/导入入口、状态/更新时间筛选和脚本入口 |
| 作品编辑器 | 已完成 | 已新增章节编辑、创作资产、短剧草稿入口、AI 助手和转短剧入口 |
| 改写对照页 | 已完成 | 已新增原文/改写稿/说明三栏对照 |
| 短剧改编工作台 | 已完成 | 已新增分集/场次编辑、场次管理、质检入口 |
| 导出预览页 | 已完成 | 已新增 Markdown/DOCX 导出预览、范围选择和下载入口 |
| 后端创作域模型 | 已完成 | 已补 schema、实体、Mapper 与 XML |
| 后端作品/章节接口 | 已完成 | 已迁移为数据库 Mapper 持久化实现 |
| 后端改写接口 | 已完成 | 已迁移为数据库 Mapper 持久化实现 |
| 后端转短剧接口 | 已完成 | 已迁移为数据库 Mapper 持久化实现，并接入可选真实 LLM |
| 质量检查 | 已完成 | 已接入可选真实 LLM，保留结构化 fallback |
| 导出 | 已完成 | 支持 Markdown/DOCX 文件下载、范围选择、改编方案、人物表、场次目录和质量报告 |
| 小说 AI 动作配置 | 已完成 | 右侧 AI 动作已升级为“动作面板 + 本次要求 + 作品级偏好 + 高级提示词”，结果默认进入草稿/建议区 |
| 端到端验收 | 已完成 | 前端构建通过，后端 JDK21 编译通过，V1 fallback 链路、等价 DOCX 和真实 DOCX 样例验收均已通过 |

## 2. 已完成

- [x] 分析 `小说skill/` 全量能力。
- [x] 分析 `剧本 (1).docx` 与 `改编-上岸 (1).docx` 的产品用途。
- [x] 创建 `产品方案-小说写改编一体化.md`。
- [x] 创建 `设计方案-小说转短剧脚本.md`。
- [x] 创建 `小说skill能力迁移清单.md`。
- [x] 创建 `小说功能实现任务清单.md`。
- [x] 创建 `小说功能实现进度记录.md`。
- [x] 创建 `小说功能V1执行说明.md`，固化 AI 继续执行方式、最小执行循环和验收命令。
- [x] 新增顶部 `创作` 导航入口。
- [x] 新增创作中心统一前端视图 `CreationView.vue`。
- [x] 新增作品库、新建作品、导入文本、作品编辑器、改写对照、短剧改编、导出预览路由。
- [x] 新增前端 `storyApi.js`。
- [x] 新增前端创作样式 `creation.css`。
- [x] 新增后端 `StoryController`。
- [x] 新增后端 `StoryWorkspaceService` 内存原型服务。
- [x] 新增创作域数据库表到 `schema.sql`。
- [x] 新增 StoryProject / StoryChapter / RewriteTask / ScriptDraft / ScriptEpisode / ScriptScene / GenerationTask 实体。
- [x] 新增创作域 MyBatis Mapper 接口和 XML。
- [x] 将 `StoryWorkspaceService` 从内存原型迁移为数据库 Mapper 持久化实现。
- [x] 完成新增创作域 Mapper XML 格式解析检查。
- [x] 新增创作域 TXT/MD/DOCX 文件导入接口。
- [x] 新增短剧分场稿 Markdown/DOCX 文件下载接口。
- [x] 前端导入弹窗支持文本粘贴和文件上传。
- [x] 前端导出预览页支持下载文件。
- [x] 新增 `StoryAiService`，写作生成与小说改写支持复用现有 `ChatLanguageModel`。
- [x] 为写作/改写 LLM 调用增加无模型或调用失败时的规则化 fallback。
- [x] 小说转短剧生成接入可选真实 LLM，并将 JSON 结果落到分集/场次表。
- [x] 短剧质量检查接入可选真实 LLM，输出总评分、逐集/逐场问题和检查项。
- [x] 新增场次 AI 修复接口，支持重写本场、补钩子、对白口语化、心理外化。
- [x] 前端短剧助手按钮接入场次 AI 修复接口。
- [x] 改写对照页新增指定要求输入和“按要求再改一次”。
- [x] 改写重试接口支持透传用户补充指令。
- [x] 新增章节版本快照表 `story_chapter_version`。
- [x] 新增 `StoryChapterVersion` 实体、Mapper 和 XML。
- [x] 保存章节、创建章节、接受改写、恢复章节时生成版本快照。
- [x] 作品编辑器右侧新增版本快照列表和恢复入口。
- [x] 导出预览页支持导出范围：全部、选中集、选中场。
- [x] 导出支持改编方案、人物表、场次目录和质量报告开关。
- [x] 后端导出 Markdown/DOCX 共享同一套内容生成逻辑。
- [x] 长任务 SSE 进度接口读取 `GenerationTask` 真实状态，并在任务不存在时返回错误事件。
- [x] 新增短剧场次新增、删除、上移、下移接口。
- [x] 删除场次后自动重排同集场次编号。
- [x] 前端短剧工作台支持新增、删除、上移、下移场次。
- [x] 作品库支持状态筛选和更新时间/标题排序。
- [x] 作品卡展示章节数、脚本草稿数和最新脚本入口。
- [x] 后端作品列表返回最新短剧脚本草稿摘要。
- [x] 写小说 AI 助手补充：生成设定、生成人物、扩写、缩写、改风格、章节审查。
- [x] 写小说生成接口补齐上述动作的 LLM Prompt 说明和无模型 fallback。
- [x] 新增文本/DOCX 导入解析预览接口。
- [x] 导入弹窗支持先解析预览，再确认创建作品。
- [x] 导入预览展示识别类型、总字数、章节数和前 20 章标题/字数。
- [x] 新增长任务状态查询、取消、重试接口。
- [x] 创作首页展示最近生成任务状态，并支持刷新、取消、重试。
- [x] 创作首页任务卡展示 Token 粗估、错误信息，并按任务状态禁用无效操作。
- [x] 导入文本覆盖首章后会生成“导入正文”章节版本快照。
- [x] 转短剧任务写入 Token 粗估记录。
- [x] 写作生成、小说改写、转短剧、短剧质检支持请求级 `useFallback` 快速规则化草稿模式。
- [x] 短剧改编工作台右侧展示改编方案摘要。
- [x] 导出预览页新增导出前格式检查、返回修复和 AI 自动修复入口。
- [x] 前端 `npm run build` 通过。
- [x] 使用 `F:\jdk21` 完成 `mvn -q -DskipTests compile` 后端编译验证。
- [x] 新增创作域 Mapper XML 格式解析检查通过。
- [x] 使用 8081 临时服务完成 V1 fallback 端到端链路：创建作品、生成章节、改写接受、转短剧、质检、Markdown/DOCX 导出。
- [x] 使用等价文本样例验证导入预览可识别“改编方案”和“短剧分场稿”。
- [x] 新增 `scripts/story_v1_docx_acceptance.ps1`，支持自动生成等价 DOCX 样例或指定真实样例路径验收。
- [x] 新增 `StoryImportClassifier` 与 `StoryImportClassifierTest`，将导入类型识别规则沉淀为可复跑单元测试。
- [x] 新增 `StoryImportDocxParsingTest`，覆盖等价 DOCX 生成、Tika 解析、类型识别的本地单测链路。
- [x] 新增 `src/test/resources/story-samples/`，沉淀删除 `小说skill/` 后仍可复用的等价样例文本。
- [x] 调整 `scripts/story_v1_docx_acceptance.ps1`，默认从正式测试样例目录生成等价 DOCX 文件。
- [x] 重新运行 `StoryImportClassifierTest`、`StoryImportDocxParsingTest`、Java 21 后端编译和 `story_v1_docx_acceptance.ps1`，均已通过。
- [x] 使用真实 `改编-上岸 (1).docx` 验证：DOCX 可上传，识别为小说源文，导入后可生成短剧改编方案。
- [x] 使用真实 `剧本 (1).docx` 验证：DOCX 可上传，并识别为短剧分场稿初稿。
- [x] 前端 dev server 已启动：`http://127.0.0.1:5173/`。
- [x] Playwright 使用系统 Chrome 完成 `/creation` 桌面与移动端渲染检查。
- [x] 新增作品级 `promptConfig` 保存到 `StoryProject.metadata.promptConfig`。
- [x] 作品编辑器右侧 AI 助手升级为动作配置/执行面板，覆盖设定、人物、大纲、续写、扩写、缩写、改风格、润色、去 AI 味、对白优化、强化冲突、章节审查。
- [x] AI 动作支持本次要求、固定偏好、动作参数和高级提示词模板。
- [x] 点击普通 AI 动作不再直接生成或覆盖正文，先打开配置面板，点击执行后结果进入可编辑草稿。
- [x] 正文类动作支持用户确认后追加到正文或替换正文。
- [x] 章节审查只输出审查建议，不修改正文。
- [x] 设定、人物、大纲仍作为创作资产编辑，AI 结果进入资产编辑框，用户确认后手动保存。
- [x] 后端 `/api/v1/story/projects/{id}/generate` 支持 `instruction`、`params`、`useCustomPrompt` 请求字段。
- [x] 后端生成提示词按默认模板、作品固定偏好、动作参数、当前资产、当前正文、本次要求合成。
- [x] 高级提示词启用后仍追加安全边界，避免泄露系统提示词和覆盖未授权内容。
- [x] 小说生成业务日志补充 `useCustomPrompt`、`instructionLength`、`params`、`sourceLength`、`resultLength`。
- [x] 前端 `npm run build` 通过。
- [x] 使用临时 `JAVA_HOME=F:\jdk21` 完成 `mvn -q -DskipTests compile` 后端编译验证。

## 3. 进行中

- [x] 使用真实 `改编-上岸 (1).docx` 与 `剧本 (1).docx` 做 DOCX 文件导入实跑验收。

## 4. 待完成：V1

### 4.1 前端

- [x] 新增 `创作` 顶部导航入口。
- [x] 新增创作首页。
- [x] 新增作品库页面。
- [x] 作品库支持类型、状态、更新时间筛选。
- [x] 作品库展示短剧脚本草稿入口。
- [x] 新增作品编辑器。
- [x] 作品编辑器支持章节版本快照查看与恢复。
- [x] 新增改写对照页。
- [x] 改写对照页支持按指定要求再改一次。
- [x] 新增短剧改编工作台。
- [x] 短剧改编工作台支持新增、删除、调整场次。
- [x] 新增导出预览页。
- [x] 导出预览页支持格式、范围、质量报告、改编方案、人物表、场次目录设置。
- [x] 增加移动端短剧工作台提示。

### 4.2 后端

- [x] 新增创作域数据库表。
- [x] 新增 StoryProject。
- [x] 新增 StoryChapter。
- [x] 新增 RewriteTask。
- [x] 新增 ScriptDraft。
- [x] 新增 ScriptEpisode。
- [x] 新增 ScriptScene。
- [x] 新增 GenerationTask。
- [x] 新增作品/章节接口。
- [x] 新增章节版本快照持久化。
- [x] 新增导入接口。
- [x] 导入接口支持解析预览和确认后创建。
- [x] 新增写小说接口。
- [x] 新增改小说接口。
- [x] 新增小说转短剧接口。
- [x] 新增短剧场次新增、删除、调整接口。
- [x] 新增质量检查接口。
- [x] 新增导出接口。
- [x] 导出接口支持 Markdown/DOCX、质量报告、改编方案、人物表、场次目录。
- [x] 新增长任务 SSE 进度接口。
- [x] 长任务支持取消任务。
- [x] 长任务支持失败重试原型。
- [x] 长任务记录 Token 使用粗估。

### 4.3 AI 链路

- [x] 写小说：设定生成。
- [x] 写小说：人物生成。
- [x] 写小说：大纲生成原型。
- [x] 写小说：章节生成原型。
- [x] 写小说：生成/润色/去 AI 味接入可选真实 LLM。
- [x] 写小说：扩写/缩写/改风格。
- [x] 写小说：章节审查。
- [x] 改小说：问题诊断原型。
- [x] 改小说：分段改写原型。
- [x] 改小说：分段改写接入可选真实 LLM。
- [x] 改小说：差异说明原型。
- [x] 转短剧：故事核提取原型。
- [x] 转短剧：人物关系提取原型。
- [x] 转短剧：情节取舍原型。
- [x] 转短剧：分集大纲生成原型。
- [x] 转短剧：短剧分场稿生成原型。
- [x] 转短剧：故事核/人物关系/情节取舍/分集/分场接入可选真实 LLM。
- [x] 短剧：质量检查原型。
- [x] 短剧：质量检查接入可选真实 LLM。
- [x] 短剧：格式修复原型。
- [x] 短剧：场次重写、补钩子、对白口语化、心理外化接入可选真实 LLM。

### 4.4 验收

- [x] 创建作品并生成一章正文。
- [x] 执行去 AI 味改写。
- [x] 三栏对照逐段接受/拒绝。
- [x] 生成短剧分集大纲。
- [x] 生成第 1 集短剧分场稿。
- [x] 执行质量检查。
- [x] 导出 Markdown。
- [x] 导出 DOCX。
- [x] 使用 `改编-上岸 (1).docx` 验证小说源文导入并生成改编方案。
- [x] 使用 `剧本 (1).docx` 验证短剧分场稿识别。

## 5. 暂不做：V1.5/V2/V3

- [ ] 长篇拆文库。
- [ ] 短篇拆文库。
- [ ] 对标资料引用。
- [ ] 文风提取。
- [ ] 多视角对抗式审查。
- [ ] 平台 rubric。
- [ ] 长篇扫榜。
- [ ] 短篇扫榜。
- [ ] 榜单采集。
- [ ] 浏览器 CDP 登录态采集。
- [ ] 封面生成。
- [ ] 项目模板中心。
- [ ] AI 角色配置中心。
- [ ] 规则中心。
- [ ] Prompt Registry 后台。
- [ ] 多人实时协作。

## 6. 风险与问题

| 风险 | 状态 | 处理建议 |
|---|---|---|
| 长文本生成上下文超限 | 未处理 | 分章节摘要、分阶段生成、任务续跑 |
| AI 改写覆盖用户原文 | 已处理 | 改写进入三栏确认，接受后更新章节并生成版本快照 |
| DOCX 导出库未确认 | 已处理 | V1 使用轻量 OpenXML 生成 DOCX 下载文件 |
| DOCX 解析稳定性 | 已处理 | 已接入 Tika 基础解析，等价 DOCX 与真实 DOCX 上传预览均已跑通，`StoryImportDocxParsingTest` 覆盖本地解析 |
| 导入类型识别规则回归 | 已处理 | 已新增单元测试覆盖改编方案、短剧分场稿、短剧优先级、短篇/长篇兜底识别，并统一读取 `src/test/resources/story-samples/` |
| 样例 DOCX 文件不可用 | 已处理 | 用户已回补 `改编-上岸 (1).docx` 和 `剧本 (1).docx` 到项目根目录，真实样例验收通过 |
| 短剧格式不稳定 | 已处理 | 已要求 LLM 输出 JSON 并加解析 fallback；等价样例和真实 `剧本 (1).docx` 均可识别为短剧分场稿 |
| Token 成本过高 | 已处理 | V1 已在转短剧任务中记录 Token 粗估；真实模型 usage 和任务前预算确认进入后续迭代 |
| 默认后端编译环境不是 Java 21 | 已处理 | 默认 `JAVA_HOME` 仍为 `F:\jdk`，但已确认可通过临时设置 `JAVA_HOME=F:\jdk21` 完成编译 |
| 后端持久化实现尚未在 Java 21 下编译验证 | 已处理 | 已使用 `F:\jdk21` 执行 `mvn -q -DskipTests compile` 并通过 |
| 转短剧与质检 AI 输出仍为规则化占位 | 已处理 | 写作、改写、转短剧、质检和场次修复均已接入可选 LLM，仍保留 fallback |
| AI 动作误触后直接覆盖正文 | 已处理 | 右侧动作已改为先打开配置面板，生成结果进入草稿区，用户明确追加/替换后才进入正文 |
| 设定/人物/大纲被当作正文生成 | 已处理 | 三类资产动作会进入资产编辑器，生成结果进入资产编辑框，保存前不写入章节正文 |

## 7. 更新规则

每次 AI 或开发者完成任务后，必须更新：

- `状态总览` 中对应模块状态。
- `已完成` 列表。
- `进行中` 列表。
- `待完成` 列表。
- 如遇问题，补充到 `风险与问题`。

状态取值：

- `待开始`
- `进行中`
- `已完成`
- `阻塞`
- `暂缓`







