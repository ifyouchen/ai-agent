# 小说功能 V1 实现任务清单

版本：V1.0  
日期：2026-06-20  
范围：仅实现写小说、改小说、小说转短剧分场稿的 V1 闭环  
参考文档：

- `产品方案-小说写改编一体化.md`
- `设计方案-小说转短剧脚本.md`
- `小说skill能力迁移清单.md`
- `小说功能V1执行说明.md`

## 1. 执行原则

- 只做 V1，不做 V1.5/V2/V3 能力。
- 不依赖 `小说skill/` 目录作为运行时路径。
- 不直接导入 `小说skill/` 下的 Hook、Agent、脚本。
- 复用当前项目已有的 LLM、SSE、RAG、审计、Token 统计、用户组织权限能力。
- 每完成一个模块，必须更新 `小说功能实现进度记录.md`。
- 所有 AI 生成内容默认先保存为草稿或建议，不直接覆盖用户正文。
- 后续继续执行时，以 `小说功能V1执行说明.md` 的最小执行循环和验证命令为准。

## 2. V1 范围

V1 包含：

- 创作中心入口。
- 作品库。
- 写小说基础链路。
- 作品导入。
- 改小说三栏对照。
- 去 AI 味。
- 小说转短剧分场稿。
- 短剧质量检查。
- Markdown/DOCX 导出。

V1 暂不包含：

- 长篇完整拆文库。
- 短篇完整拆文库。
- 多 Agent 对抗式审查。
- 长篇/短篇扫榜。
- 浏览器 CDP 采集。
- 小说封面生成。
- 项目模板中心。
- AI 角色配置中心。
- 规则中心后台。
- 多人实时协作。

## 3. 最小任务清单

### 3.1 准备与约束

- [x] 阅读现有前端路由、布局和组件风格。
- [x] 阅读现有后端 Controller、Service、实体、Mapper、schema 风格。
- [x] 确认当前项目数据库迁移/建表方式。
- [x] 确认当前项目 LLM 流式调用方式。
- [x] 确认当前项目文件上传与 DOCX 解析方式。

### 3.2 前端：导航与创作首页

- [x] 顶部导航新增 `创作` 入口。
- [x] 新增 `/creation` 创作首页路由。
- [x] 创作首页展示最近作品、进行中任务、快捷入口。
- [x] 快捷入口包含新建长篇、新建短篇、导入小说、改写文本、转短剧。
- [x] 空状态引导用户创建或导入作品。

### 3.3 前端：作品库

- [x] 新增 `/creation/projects` 作品库路由。
- [x] 展示小说项目、改编项目、短剧脚本草稿。
- [x] 支持类型、状态、更新时间筛选。
- [x] 支持新建作品。
- [x] 支持导入文本/DOCX。
- [x] 支持继续写作、继续改编、查看脚本。

### 3.4 前端：作品编辑器

- [x] 新增 `/creation/projects/:id/editor` 作品编辑器路由。
- [x] 左侧展示作品树、设定、人物、大纲、章节、短剧草稿。
- [x] 中间提供章节正文编辑区。
- [x] 右侧提供 AI 助手操作。
- [x] 支持续写、扩写、缩写、润色、去 AI 味、强化冲突、对白优化、转短剧。
- [x] 支持自动保存或手动保存。
- [x] 支持版本状态展示。

### 3.5 前端：改小说三栏对照

- [x] 新增 `/creation/rewrite/:taskId` 改写对照路由。
- [x] 左栏展示原文。
- [x] 中栏展示 AI 改写稿。
- [x] 右栏展示修改说明、问题诊断、操作按钮。
- [x] 支持接受本段、拒绝本段、再改一次、指定要求再改。
- [x] 支持保存为新版本。
- [x] 支持回到作品编辑器。

### 3.6 前端：短剧改编工作台

- [x] 新增 `/creation/scripts/:draftId` 短剧改编工作台路由。
- [x] 左栏展示原小说章节或导入源文。
- [x] 中左栏展示分集目录和场次目录。
- [x] 中栏展示当前集/当前场分场稿编辑器。
- [x] 右栏展示 AI 助手、质量检查、角色设定。
- [x] 支持新增、删除、调整场次。
- [x] 支持重写本集、重写本场、补钩子、对白口语化、心理外化。
- [x] 移动端提示使用桌面端完成完整改编。

### 3.7 前端：导出预览

- [x] 新增 `/creation/scripts/:draftId/export` 导出预览路由。
- [x] 支持导出范围选择：全部、选中集、选中场。
- [x] 支持导出格式：Markdown、DOCX。
- [x] 支持是否包含质量报告、改编方案、人物表、场次目录。
- [x] 导出前展示格式检查和质量问题。
- [x] 支持继续导出、返回修复、AI 自动修复可修复项。

### 3.8 后端：创作域基础模型

- [x] 新增作品项目模型 `StoryProject`。
- [x] 新增章节模型 `StoryChapter`。
- [x] 新增改写任务模型 `RewriteTask`。
- [x] 新增短剧草稿模型 `ScriptDraft`。
- [x] 新增分集模型 `ScriptEpisode`。
- [x] 新增场次模型 `ScriptScene`。
- [x] 新增长任务模型 `GenerationTask`。
- [x] 新增必要数据库表和索引。

### 3.9 后端：作品与章节接口

- [x] `POST /api/v1/story/projects` 创建作品。
- [x] `GET /api/v1/story/projects` 查询作品列表。
- [x] `GET /api/v1/story/projects/{id}` 查询作品详情。
- [x] `PUT /api/v1/story/projects/{id}` 更新作品。
- [x] `POST /api/v1/story/projects/{id}/chapters` 新增章节。
- [x] `PUT /api/v1/story/chapters/{chapterId}` 保存章节。
- [x] `GET /api/v1/story/chapters/{chapterId}/versions` 查询版本。
- [x] `POST /api/v1/story/chapters/{chapterId}/restore` 恢复版本。

### 3.10 后端：导入接口

- [x] 支持粘贴文本导入。
- [x] 支持 TXT/Markdown 导入。
- [x] 支持 DOCX 导入。
- [x] 自动识别章节边界。
- [x] 自动统计字数和章节数。
- [x] 自动识别长篇/短篇。
- [x] 返回解析预览，用户确认后创建项目。

### 3.11 后端：写小说接口

- [x] 生成故事设定。
- [x] 生成人物。
- [x] 生成大纲。
- [x] 生成章节正文。
- [x] 续写章节。
- [x] 扩写/缩写/改风格。
- [x] 章节审查。
- [x] AI 结果保存为草稿或建议。

### 3.12 后端：改小说接口

- [x] `POST /api/v1/story/rewrite` 创建改写任务。
- [x] `GET /api/v1/story/rewrite/{taskId}` 查询改写任务。
- [x] `POST /api/v1/story/rewrite/{taskId}/accept` 接受改写结果。
- [x] `POST /api/v1/story/rewrite/{taskId}/retry` 重新改写。
- [x] 支持基础润色、去 AI 味、爽点增强、节奏压缩、情绪强化、对白口语化、题材迁移。
- [x] 生成段落级差异说明。
- [x] 接受结果后生成新版本。

### 3.13 后端：小说转短剧接口

- [x] `POST /api/v1/story/script/convert` 创建转短剧任务。
- [x] `GET /api/v1/story/script/tasks/{taskId}` 查询任务状态。
- [x] `GET /api/v1/story/script/drafts/{draftId}` 查询脚本草稿。
- [x] `PUT /api/v1/story/script/scenes/{sceneId}` 保存场次。
- [x] 支持故事核提取。
- [x] 支持人物关系提取。
- [x] 支持情节取舍。
- [x] 支持分集大纲生成。
- [x] 支持短剧分场稿生成。

### 3.14 后端：短剧质量检查

- [x] `POST /api/v1/story/script/drafts/{draftId}/quality-check` 执行质量检查。
- [x] 检查格式完整度。
- [x] 检查前 3 秒钩子。
- [x] 检查冲突密度。
- [x] 检查结尾钩子。
- [x] 检查对白口语化。
- [x] 检查小说残留。
- [x] 检查心理活动外化。
- [x] 检查可拍性。
- [x] 输出总评分、主要问题、逐集问题、逐场问题、可自动修复项、需人工判断项。

### 3.15 后端：导出

- [x] `POST /api/v1/story/script/drafts/{draftId}/export` 导出脚本。
- [x] 支持 Markdown 导出。
- [x] 支持 DOCX 导出。
- [x] 支持导出质量报告。
- [x] 支持导出改编方案。
- [x] 支持导出人物表。
- [x] 支持导出场次目录。

### 3.16 长任务与流式进度

- [x] 复用现有 SSE 能力。
- [x] 新增 `GET /api/v1/story/tasks/{taskId}/stream`。
- [x] 生成任务展示当前步骤。
- [x] 支持失败重试。
- [x] 支持取消任务。
- [x] 记录 Token 使用。
- [x] 记录错误信息。

### 3.17 验收与测试

- [x] 新建作品并生成一章正文。
- [x] 对章节执行去 AI 味改写。
- [x] 三栏对照逐段接受/拒绝。
- [x] 选择章节生成短剧分集大纲。
- [x] 生成第 1 集短剧分场稿。
- [x] 执行短剧质量检查。
- [x] 导出 Markdown。
- [x] 导出 DOCX。
- [x] 导入 `改编-上岸 (1).docx` 并生成改编方案。
- [x] 导入 `剧本 (1).docx` 并识别为短剧分场稿初稿。
> 真实 DOCX 样例已回补并完成验收：`改编-上岸 (1).docx` 作为小说源文导入后成功生成短剧改编方案；`剧本 (1).docx` 已识别为短剧分场稿初稿。验收命令见 `小说功能V1执行说明.md`。

## 4. 推荐执行顺序

1. 建数据表和基础后端模型。
2. 建作品项目与章节 CRUD。
3. 建前端创作入口和作品库。
4. 建作品编辑器。
5. 接入写小说基础 AI 生成。
6. 建改写任务和三栏对照。
7. 建短剧草稿、分集、场次模型和接口。
8. 建短剧改编工作台。
9. 接入小说转短剧生成链路。
10. 建质量检查。
11. 建导出。
12. 做端到端验收。

## 5. 暂缓任务池

以下任务已经纳入产品路线，但不进入 V1 实现：

- 长篇拆文库。
- 短篇拆文库。
- 对标资料引用。
- 文风提取。
- 多视角对抗式审查。
- 平台 rubric。
- 长篇扫榜。
- 短篇扫榜。
- 榜单采集。
- 浏览器 CDP 登录态采集。
- 封面生成。
- 项目模板中心。
- AI 角色配置中心。
- 规则中心。
- Prompt Registry 后台。




