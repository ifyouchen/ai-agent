# 小说功能 V1 执行说明

版本：V1.0  
日期：2026-06-20  
用途：说明如何让 AI 或开发者按 `小说功能实现任务清单.md` 继续执行 V1，并同步维护 `小说功能实现进度记录.md`。

## 1. 执行入口

执行 V1 时，只需要给 AI 明确一句话：

```text
按 小说功能实现任务清单.md 开始实现 V1，并持续更新 小说功能实现进度记录.md
```

AI 应先读取：

- `小说功能实现任务清单.md`
- `小说功能实现进度记录.md`
- `产品方案-小说写改编一体化.md`
- `设计方案-小说转短剧脚本.md`
- `小说skill能力迁移清单.md`

执行时以任务清单为主线，完成一个模块后立即更新进度记录，不能只在最后统一补记录。

## 2. 最小执行循环

每一轮开发按以下循环推进：

1. 从 `小说功能实现任务清单.md` 找到第一个未完成 V1 项。
2. 阅读现有代码结构，确认前端、后端、数据库、API 的落点。
3. 实现最小可用功能，不扩展到 V1.5/V2/V3。
4. 运行对应验证命令。
5. 更新 `小说功能实现任务清单.md` 的勾选状态。
6. 更新 `小说功能实现进度记录.md` 的已完成、进行中、风险与问题。
7. 如果遇到阻塞，写清楚阻塞原因、影响范围和恢复条件。

## 3. 本地验证命令

前端构建：

```powershell
Set-Location frontend
npm run build
```

后端编译需要 Java 21：

```powershell
$env:JAVA_HOME='F:\jdk21'
$env:Path='F:\jdk21\bin;' + $env:Path
& 'F:\software\apache-maven-3.9.0\bin\mvn.cmd' -q -DskipTests compile
```

小说导入规则单测：

```powershell
$env:JAVA_HOME='F:\jdk21'
$env:Path='F:\jdk21\bin;' + $env:Path
& 'F:\software\apache-maven-3.9.0\bin\mvn.cmd' -q -Dtest=StoryImportClassifierTest,StoryImportDocxParsingTest test
```

启动后端临时服务：

```powershell
$env:JAVA_HOME='F:\jdk21'
$env:Path='F:\jdk21\bin;' + $env:Path
& 'F:\software\apache-maven-3.9.0\bin\mvn.cmd' -q spring-boot:run '-Dspring-boot.run.arguments=--server.port=8081'
```

运行 DOCX 导入预览验收：

```powershell
./scripts/story_v1_docx_acceptance.ps1 -BaseUrl http://127.0.0.1:8081
```

默认模式会读取 `src/test/resources/story-samples/` 下的等价文本，并在 `target/story-v1-fixtures/` 生成等价 DOCX 样例，验证“改编方案”和“短剧分场稿”识别规则。

如果原始样例文件已恢复，使用真实文件验收：

```powershell
./scripts/story_v1_docx_acceptance.ps1 `
  -BaseUrl http://127.0.0.1:8081 `
  -AdaptationDocx "F:\path\to\改编-上岸 (1).docx" `
  -ScriptDocx "F:\path\to\剧本 (1).docx"
```

## 4. 当前 V1 验收口径

V1 完成需要满足：

- 可以创建小说作品并生成章节。
- 可以保存章节并生成版本快照。
- 可以执行小说改写和去 AI 味。
- 可以在三栏对照页逐段接受、拒绝和再次改写。
- 可以把小说转为短剧分集和分场稿。
- 可以编辑、增删、调整短剧场次。
- 可以执行短剧质量检查。
- 可以导出 Markdown 和 DOCX。
- 可以导入 DOCX 并识别改编方案。
- 可以导入 DOCX 并识别短剧分场稿初稿。

其中最后两项如果原始 `小说skill/` 样例已经被删除，必须保留未完成状态；可以用脚本生成的等价 DOCX 证明规则可用，但不能替代真实样例验收。

## 5. 删除 `小说skill/` 后的约束

删除 `小说skill/` 后，项目不应再依赖该目录作为运行时或开发时路径。后续小说功能的依据是：

- 产品文档：`产品方案-小说写改编一体化.md`
- 设计文档：`设计方案-小说转短剧脚本.md`
- 能力基线：`小说skill能力迁移清单.md`
- 执行清单：`小说功能实现任务清单.md`
- 进度记录：`小说功能实现进度记录.md`
- 验收脚本：`scripts/story_v1_docx_acceptance.ps1`
- 等价样例：`src/test/resources/story-samples/`



