# Vite dev server proxy target. Keep this pointing at the Spring Boot backend.
VITE_BACKEND_TARGET=http://localhost:8080

# Browser-visible API base URL for production/static builds.
# Leave empty in local dev so /api is handled by the Vite proxy.
VITE_API_BASE_URL=
