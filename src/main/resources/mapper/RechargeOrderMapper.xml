<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.aiagent.billing.mapper.RechargeOrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO recharge_order (
            order_no, user_id, package_code, amount_cents, pay_channel,
            status, provider_trade_no, pay_qr_content, expire_at
        ) VALUES (
            #{orderNo}, #{userId}, #{packageCode}, #{amountCents}, #{payChannel},
            #{status}, #{providerTradeNo}, #{payQrContent}, #{expireAt}
        )
    </insert>

    <select id="findByOrderNo" resultType="com.example.aiagent.billing.entity.RechargeOrder">
        SELECT *
        FROM recharge_order
        WHERE order_no = #{orderNo}
    </select>

    <select id="findByOrderNoForUpdate" resultType="com.example.aiagent.billing.entity.RechargeOrder">
        SELECT *
        FROM recharge_order
        WHERE order_no = #{orderNo}
        FOR UPDATE
    </select>

    <select id="listByUser" resultType="com.example.aiagent.billing.entity.RechargeOrder">
        SELECT *
        FROM recharge_order
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="markPaid">
        UPDATE recharge_order
        SET status = 'PAID',
            provider_trade_no = #{providerTradeNo},
            paid_at = #{paidAt},
            updated_at = NOW()
        WHERE order_no = #{orderNo}
          AND status != 'PAID'
    </update>

</mapper>
