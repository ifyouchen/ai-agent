<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aiagent.memory.UserMemoryMapper">

    <select id="findByUserId" resultType="com.example.aiagent.memory.UserMemory">
        SELECT id, user_id, facts_text, updated_at
        FROM user_memory
        WHERE user_id = #{userId}
    </select>

    <insert id="upsert">
        INSERT INTO user_memory (user_id, facts_text, updated_at)
        VALUES (#{userId}, #{factsText}, NOW())
        ON CONFLICT (user_id) DO UPDATE
            SET facts_text = #{factsText},
                updated_at = NOW()
    </insert>

</mapper>

