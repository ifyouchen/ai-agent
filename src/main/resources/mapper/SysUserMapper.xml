<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.aiagent.security.mapper.SysUserMapper">

    <resultMap id="SysUserResultMap" type="com.example.aiagent.security.entity.SysUser">
        <id     property="id"           column="id"/>
        <result property="userId"       column="user_id"/>
        <result property="username"     column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="roles"        column="roles"/>
        <result property="enabled"      column="enabled"/>
        <result property="createdAt"    column="created_at"/>
        <result property="updatedAt"    column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="SysUserResultMap">
        SELECT id, user_id, username, password_hash, roles, enabled, created_at, updated_at
        FROM biz_user_account
        WHERE username = #{username}
    </select>

    <select id="findByUserId" resultMap="SysUserResultMap">
        SELECT id, user_id, username, password_hash, roles, enabled, created_at, updated_at
        FROM biz_user_account
        WHERE user_id = #{userId}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM biz_user_account
        WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_user_account (
            user_id, username, password_hash, roles, enabled,
            balance, membership_level, points,
            created_at, updated_at
        ) VALUES (
            #{userId}, #{username}, #{passwordHash}, #{roles}, #{enabled},
            0, 'NORMAL', 0,
            NOW(), NOW()
        )
    </insert>

</mapper>

