<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.aiagent.billing.mapper.BillingLedgerMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO billing_ledger (
            ledger_no, user_id, type, amount_cny, balance_after_cny,
            ref_type, ref_id, idempotency_key, remark
        ) VALUES (
            #{ledgerNo}, #{userId}, #{type}, #{amountCny}, #{balanceAfterCny},
            #{refType}, #{refId}, #{idempotencyKey}, #{remark}
        )
        ON CONFLICT (idempotency_key) DO NOTHING
    </insert>

    <select id="listByUser" resultType="com.example.aiagent.billing.entity.BillingLedger">
        SELECT *
        FROM billing_ledger
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>
